name: Backend CI/CD

# Runs the test suite and coding-style check on every push and pull request that
# touches the backend, then deploys to the VPS when the change lands on main.
on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "deploy.sh"
      - "docker-compose.yml"
      - "docker-compose.prod.yml"
      - ".github/workflows/backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - "deploy.sh"
      - "docker-compose.yml"
      - "docker-compose.prod.yml"
      - ".github/workflows/backend.yml"

# A newer push to the same ref cancels an in-flight run so deploys never overlap.
concurrency:
  group: backend-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: backend

jobs:
  test:
    name: Test & lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # PHP 8.4 matches the production Dockerfile (php:8.4-fpm-alpine).
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          extensions: pdo_sqlite, intl, pcntl
          coverage: none

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: backend/vendor
          key: composer-${{ runner.os }}-${{ hashFiles('backend/composer.lock') }}
          restore-keys: composer-${{ runner.os }}-

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Prepare environment
        run: |
          cp .env.example .env
          php artisan key:generate

      # Fails the build on any style violation without rewriting files.
      - name: Check code style (Pint)
        run: vendor/bin/pint --test

      # Tests run against the in-memory SQLite database configured in phpunit.xml.
      - name: Run tests
        run: php artisan test

  deploy:
    name: Deploy to VPS
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    # The DEPLOY_* secrets live on this GitHub environment, not on the repo.
    environment: Production
    steps:
      - name: Deploy over SSH
        # This job does not check out the repo, so override the backend default.
        working-directory: ${{ github.workspace }}
        env:
          SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          SSH_KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
          SSH_HOST: ${{ secrets.DEPLOY_HOST }}
          SSH_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_deploy
          printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/id_deploy ~/.ssh/known_hosts
          ssh -o BatchMode=yes -o IdentitiesOnly=yes -o ConnectTimeout=15 \
            -i ~/.ssh/id_deploy "$SSH_USER@$SSH_HOST" \
            'cd /opt/personal && ./deploy.sh'
